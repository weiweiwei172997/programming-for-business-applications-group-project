# AI Prompt Evolution Log - GymPath

Use this file to record how AI assistance shaped the project.

| Timestamp | Prompt / Request | AI Output Kept | AI Output Changed Or Discarded | Decision Justification |
|---|---|---|---|---|
| 2026-05-14 | Created research, PRD, technical design, and agent instructions for GymPath | Project direction, Streamlit/Python/SQLite stack, MVP scope, safety rules, AI workflow | Native app and WeChat Mini Program were postponed | Web app first best satisfies Python course requirements and allows fast MVP delivery |
| 2026-05-14 | Read AGENTS.md, propose Phase 1 plan, wait for approval, then implement | Created requirements, project.py, pytest tests, data files, first Streamlit app shell, README | Full SQLite community and AI API integration were left for the next slice | Core Python logic and tests should be stable before adding persistence and AI complexity |
| 2026-05-14 | Make the first version Chinese | Chinese UI labels, guidance, plan display, feedback flow, nutrition/progress wording, and Chinese knowledge/exercise data | Internal function names and enum values stayed English | Keeping internal logic English avoids breaking tests while making the visible app usable for Chinese-speaking users |
| 2026-05-14 | Use shadcn and frontend-design skills to redesign the frontend with a high-end fitness fashion feel | Rebuilt app.py visual system with dark athletic brand direction, electric green accents, premium cards, status strip, training matrix, and dashboard-style sections | Did not add a React/shadcn build system because the MVP core must remain Python/Streamlit | The project benefits from shadcn composition principles without adding a second frontend stack before the Python MVP is stable |
| 2026-05-14 | Redesign frontend again using only black/white/gray colors and native components | Rewrote app.py with native Streamlit components only and added `.streamlit/config.toml` monochrome theme | Removed custom HTML/CSS injection and colored electric-green visual language | User explicitly requested native components and monochrome design, so Streamlit-native composition is safer and clearer |
| 2026-05-14 | Asked whether React/Next should be used if it creates a better frontend while keeping Python core logic | Added `api.py` FastAPI wrapper and a `frontend/` Next/React interface using only black, white, and gray with native HTML controls | Did not force shadcn/ui runtime components because the user requested native components and the project had no existing shadcn configuration | React/Next gives a much higher UI ceiling than Streamlit while `project.py` still satisfies Python course requirements |
| 2026-05-14 | Requested all exercise teaching links use Douyin or Bilibili instead of YouTube, prioritizing specified fitness creators | Updated core plan generation to output Bilibili and Douyin search links with preferred creators, updated React/Streamlit labels, replaced static exercise JSON links, and added tests | Did not hardcode individual video URLs because availability/search ranking can change | Platform search links stay current while keeping every teaching link on Chinese video platforms |
| 2026-05-15 | Requested teaching links jump directly to specific pre-selected videos instead of search pages | Replaced generated teaching links with a fixed Bilibili video mapping per exercise, added video title/creator/platform metadata, removed search links from UI and static JSON, and updated tests | Douyin direct links were not used because reliable direct video IDs were harder to verify than Bilibili video pages | Direct video links create a smoother demo experience and avoid sending users into noisy search results |
| 2026-05-15 | Requested beginner users use a four-day split instead of full-body training, with four specified Bilibili training videos and warm-up videos by body part | Rebuilt beginner plan generation into chest/shoulders/triceps, back/biceps, lower body, rest day, shoulders/arms; attached the four supplied training videos and fixed Bilibili warm-up videos; updated React UI and tests | The weekly training day slider is overridden for beginner plans because the requested beginner plan is explicitly four sessions | Beginner experience now follows a concrete video-led plan instead of a generic full-body template |
| 2026-05-15 | Requested video cards stop showing author/source/brief descriptions and make all set rests at least two minutes with flexible longer rests for heavier or more fatiguing work | Removed video title/creator/platform metadata from generated plans and UI display, added rest guidance, enforced minimum 120-second rests, and added feedback-based longer rest recommendations | Kept direct video buttons because they are still needed for teaching, but removed surrounding explanatory metadata | The interface is cleaner and the training rule now matches bodybuilding-style rest needs better than short generic intervals |
| 2026-05-15 | Provided a previous fat-loss macro formula and asked to add the logic to the web app nutrition section | Added `generate_diet_plan()` with 2x bodyweight carbs, 0.8x bodyweight fat, 1.3x bodyweight protein, weekly high/medium/low carb cycling, and meal timing rules; exposed it through `/api/nutrition` and rendered it in the React nutrition page | Did not copy another app's hidden content; used the user's provided formula and made it weight-based instead of hardcoding only 93kg | The app now has a concrete, testable fat-loss diet feature that can adapt to different user body weights |
| 2026-05-15 | Requested multiple selectable fat-loss diet plans and added the Orange carb-tapering fat-loss formula | Added a fat-loss plan selector, kept the previous plan as `凯圣王碳循环减脂`, added `橙子碳水渐降减脂` with BMR calculation, training burn coefficients, standard/sensitive macro ratios, target-weight timeline, and carb-reduction protocol | Did not make the Orange plan the only fat-loss option because the app now needs plan choice after selecting fat loss | Users can compare and run different fat-loss strategies while keeping the calculations testable in Python |
| 2026-05-15 | Requested repeated rest guidance be removed from every exercise card and shown only at the beginning of each training day | Removed exercise-level `rest_guidance`, added a day-level `rest_policy`, and rendered it once before the day's video/warm-up/exercise list | Kept numeric rest seconds on each exercise because users still need exact set timing | The plan is cleaner and avoids repeating the same instruction across every movement card |
| 2026-05-15 | Requested removal of broad exercise cues such as "keep shoulder blades stable and control range" | Removed the React exercise cue renderer and the Streamlit fallback exercise note renderer, so exercise cards now focus on exercise name, dose, rest, muscle group, and video link | Kept the underlying `notes` data in Python for future adaptive logic, but it is no longer displayed as generic instruction text | The UI is less generic and avoids advice that feels too broad or AI-generated |
| 2026-05-15 | Requested three strength-gain plans based on video summaries for beginners, advanced lifters, and all users | Added selectable strength plans: `小白A/B轮线性力量`, `老手线性5x5增力`, and `全人群5x5三分化`; each returns training days, strength-plan logic, progression rules, stall strategy, and warnings from Python | Did not display generic movement cues; kept the plan focused on programming logic, loading rules, and training structure | Strength training now has real periodization logic instead of being treated like a generic hypertrophy plan |

| 2026-05-15 | Requested body-data controls be removed from all sections except training plan and nutrition | Added conditional React rendering so the body-data panel appears only on the plan and nutrition tabs, with a single-column setup layout elsewhere | Kept the training-profile panel visible because it still controls plan generation and app state | This reduces clutter in pain, feedback, progress, and knowledge sections while preserving body data where it affects calculations |
| 2026-05-15 | Requested muscle-gain plans be renamed and expanded, and all full-body training content removed | Renamed the original beginner hypertrophy plan to `谭成义新手跟练`, added `谭成义+凯圣王三分化` and `橙子增肌计划`, exposed a React selector, added API support, and removed legacy full-body templates/labels | Did not turn these into strength-gain options because the user specifically requested them under hypertrophy | Muscle gain now has real selectable programming instead of one generic path, and the app better matches the requested creator-style plans |
| 2026-05-15 | Requested the fat-loss module learn the screenshot's diet logging flow, search nutrition data online, support multi-food meals, and connect totals to the existing fat-loss plan | Added a Python food macro library, meal-total calculation functions, API endpoints, and a React meal logger with selectable foods per meal, per-meal macro totals, daily selected totals, remaining target macros, and source notes | Did not scrape the mini-program itself; implemented the visible workflow pattern and used public nutrition data estimates instead | This adds practical diet execution to the fat-loss plan instead of only showing target macros |
| 2026-05-15 | Requested more healthy fitness foods, custom grams, and dry/cooked weight switching | Expanded the food macro library to 42 foods, added per-100g state data, supported dry/cooked and raw/cooked options, upgraded React selected-food rows with gram inputs and state selectors, and kept backend meal calculations testable | Did not make every food have both states when the distinction is not meaningful, such as whey or olive oil | Users can now log realistic portions instead of being locked to fixed one-serving buttons |
| 2026-05-15 | Requested removal of training-profile/body-data panels outside training and nutrition, interactive pain body map, progress line chart with body-fat percentage, and more beginner knowledge cards | Added a joint-clickable body map to the pain module, expanded `/api/pain` with joint-specific substitution/relief/rehab/video guidance, added body-fat trend support, replaced the progress meter with a line chart, and expanded the knowledge database | Did not add a medical diagnosis flow; kept pain guidance cautious, non-diagnostic, and framed as training modification support | The app now feels more interactive and educational while staying safer and more demo-ready |
| 2026-05-16 | Requested clearer trend-line distinction, custom dates, and a less abstract human joint image similar to a muscle anatomy chart, with future 3D modeling direction | Updated the progress chart to use distinct solid/dashed/dotted line styles with different point markers, clarified editable dates, and replaced the abstract joint map with an original front/back muscle anatomy plate plus a 3D roadmap panel | Did not copy the provided reference image directly and did not add a full Three.js model in this slice | Original assets avoid copyright risk, and the MVP gets a much clearer anatomy interaction without adding fragile 3D complexity yet |
| 2026-05-16 | Asked how target calories/protein are calculated, renamed fat-loss diet logging to diet logging, and provided a full nutrition guide for muscle-gain/strength-gain goals | Added performance nutrition logic for muscle gain and strength gain: Harvard-style BMR, lifestyle burn, training burn, training-day surplus, rest-day deficit with BMR floor, 5:2.5:2.5 macro ratio, UI explanation cards, API response updates, and pytest coverage | Did not apply this guide to fat-loss plan selection because fat loss already has selectable Kai Sheng Wang carb cycling and Orange carb-tapering plans | Goal-specific nutrition makes the app more coherent: fat loss keeps cutting logic, while muscle gain and strength gain now show training-performance fuel targets |
| 2026-05-16 | Requested removing the fitness goal from the training profile, replacing the self-drawn muscle map with a real online anatomy image, and adding home upper/lower training for the health goal | Removed the visible `体能` goal option, added a health-goal upper/lower home bodyweight schedule using the supplied Bilibili videos, replaced the SVG anatomy drawing with a Wikimedia/OpenStax image plus clickable joint hotspots, and added pytest coverage | Kept `general_fitness` in backend validation for compatibility but removed it from the user-facing selector | This keeps the interface focused while giving health-oriented users a low-barrier non-gym path and a clearer real anatomy reference |
| 2026-05-16 | Requested removing weekly training-day selection from training profile and adding a 7-day check-in incentive for supplement lottery prizes | Removed the weekly training-days slider from the setup panel, changed readiness scoring to use session duration and level only, added `get_checkin_reward_status()`, exposed `/api/checkin-reward`, and added a feedback-page check-in lottery card for protein powder, creatine, and related prizes | Kept `days_per_week` internally for plan generation defaults and existing API compatibility | The interface is simpler while the habit loop becomes more motivating and demo-friendly |
| 2026-05-16 | Requested integrating an AI API for fitness Q&A using the OpenAI SDK with DeepSeek base URL | Added `get_ai_fitness_reply()`, `/api/ai-chat`, `.env.example`, and a React `AI问答` tab with chat history, example prompts, DeepSeek metadata, and local fallback answers | Did not hardcode the provided key-like string in source code; the backend reads `DEEPSEEK_API_KEY` from environment or `.env` | Keeping secrets out of code is safer, while fallback mode keeps the course demo usable even without a valid API key |
| 2026-05-16 | Requested a document compliance check, then asked to update documents and gradually fix missing items | Synchronized PRD, Tech Design, AGENTS, agent docs, README, and tool configs to the real FastAPI + React/Next architecture; added SQLite persistence for measurements, check-ins, and workout feedback; expanded AI coach context | Did not rewrite the historical research file because it records the original Streamlit recommendation and should remain as research history | Keeping source documents aligned with shipped behavior improves collaborator onboarding, course grading clarity, and future AI-agent reliability |
| 2026-05-16 | Add photo posting to the community section | Added optional community image upload with FastAPI multipart handling, local SQLite image references, local file storage, and React preview/display in the feed | Did not add cloud storage or multi-image albums | Local single-image upload is enough for the MVP, works through the public tunnel, and avoids extra paid services or privacy risk |
| 2026-05-16 | Make community photos openable as large images and keep the public link updated | Changed feed photos to full, uncropped display; added a click-to-open image preview overlay; restarted the local app and verified the public tunnel still returns 200 | Did not add a full gallery system | A simple lightbox gives the expected social-app interaction without adding unnecessary complexity |

## How To Use This Log

After every meaningful AI interaction, add one row:

- What did you ask?
- What useful output did AI provide?
- What did you reject or modify?
- Why was that decision good for the project?

Keep entries concise. The goal is to show thoughtful AI use, not paste entire chats.
